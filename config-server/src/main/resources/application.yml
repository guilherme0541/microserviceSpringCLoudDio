
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/guilherme0541/microserviceSpringCloudDio-config.git
          default-label: main
          
  application:
    name: configserver